1. sql: 
	database: new_data
	table : can find in mysql structure
2. script: wrote a python script which calls the api every 1 hour for all the files present in the directory.
	upload: to upload files of a folder copy this script to the directory and run the script or change the of dir to 		the path of the directory 
3. api: 
	install dependencies: run "npm install" in terminal to install all the dependencies.
	sql connection: change mysql port, user and password.
	start: run "nodemon index.js" to start the service.